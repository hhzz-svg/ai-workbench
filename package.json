{
  "name": "skill-workbench",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:api": "python -m uvicorn app.main:app --app-dir backend --reload --host 127.0.0.1 --port 8000",
    "dev:web": "npm --prefix frontend run dev",
    "test:backend": "python -m pytest backend/tests -q",
    "test:frontend-localization": "node frontend/tests/localization.test.mjs",
    "build:web": "npm --prefix frontend run build"
  }
}
