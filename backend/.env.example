# Comma-separated origins that may call the FastAPI backend from a hosted frontend.
# Keep the local defaults implicit; add only production or preview origins here.
SKILL_WORKBENCH_CORS_ORIGINS=https://ai-workbench.pages.dev,https://your-domain.com
